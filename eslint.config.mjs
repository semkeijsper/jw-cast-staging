// Generated config from @nuxt/eslint is at .nuxt/eslint.config.mjs (run `pnpm postinstall` first).
// Prettier handles all formatting — ESLint only enforces code quality rules.
import withNuxt from './.nuxt/eslint.config.mjs';
import configPrettier from 'eslint-config-prettier';

export default withNuxt(configPrettier);
